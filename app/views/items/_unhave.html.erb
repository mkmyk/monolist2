<%= link_to "" , 
  ownership_path( current_user.haves.find_by(:item_id => item.id) , 
    item_id: item.id , type: "Have", id: current_user.id) ,
  :class => "have-#{item.id} btn-have btn btn-warning btn-sm have_it",
  :remote => true,
  :method => :delete %> 
